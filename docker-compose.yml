services:
  firefly:
    image: firefly-blog:local
    build:
      context: .
      dockerfile: Dockerfile
      args:
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        NO_PROXY: ${NO_PROXY:-}
    ports:
      - "${FIREFLY_PORT:-80}:80"
    restart: unless-stopped
